import React from 'react';
import { Box, Typography } from '@mui/material';

const SaltRequests = () => {
  return (
    <Box sx={{ mt: 4 }}>
      <Typography variant="h4" gutterBottom>
        Salt Requests
      </Typography>
      <Typography variant="body1">
        Salt requests will be displayed here. This is a placeholder component.
      </Typography>
    </Box>
  );
};

export default SaltRequests; 